using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball_Off : MonoBehaviour
{
    
    void Start()
    {
        Destroy(gameObject,0.6f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
